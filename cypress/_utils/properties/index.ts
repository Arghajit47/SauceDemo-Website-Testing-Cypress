const properties = {
  LOGIN_URL: Cypress.config().LOGIN_URL,
  USERNAME_1: Cypress.config().USERNAME_1,
  USERNAME_2: Cypress.config().USERNAME_2,
  USERNAME_3: Cypress.config().USERNAME_3,
  USERNAME_4: Cypress.config().USERNAME_4,
  PASSWORD: Cypress.config().PASSWORD,
  INVALID_USERNAME: Cypress.config().INVALID_USERNAME,
  INVALID_PASSWORD: Cypress.config().INVALID_PASSWORD,
  INVENTORY_URL: Cypress.config().INVENTORY_URL,
  FIRSTNAME: Cypress.config().FIRSTNAME,
  LASTNAME: Cypress.config().LASTNAME,
  POSTAL_CODE: Cypress.config().POSTAL_CODE,
  CHECKOUT_COMPLETE_URL: Cypress.config().CHECKOUT_COMPLETE_URL,
  CHECKOUT_INFO_URL: Cypress.config().CHECKOUT_INFO_URL,
  CHECKOUT_OVERVIEW_URL: Cypress.config().CHECKOUT_OVERVIEW_URL,
};

export default properties;
